---
title: "Upload a new document version"
sidebarTitle: "Upload version"
description: "Upload an evidence file as base64 JSON: PDF, Word, Excel, image or video, at most 10 MB. The returned path is what you attach as a control or task answer's evidence. Deduplicates by SHA256 checksum: if the same content already exists for this artifact, returns the existing version (200) instead of creating a new one (201). Trims old versions beyond 10 per artifact."
openapi: post /api/v1/v-ciso/organizations/{organizationid}/workflows/{workflowid}/documents/{artifactid}/versions
---
