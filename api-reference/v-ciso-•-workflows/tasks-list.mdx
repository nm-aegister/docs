---
title: "List the organization's tasks"
sidebarTitle: "List Tasks"
description: "Discover every task (table and question) with its type, so a script can locate what to fill, then call the per-task spec / answers / import endpoints. Optional ?type=table|questions filter."
openapi: get /api/v1/v-ciso/organizations/{organizationid}/tasks
---
