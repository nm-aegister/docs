---
title: "List a workflow's documents"
sidebarTitle: "List documents"
description: "Discover every artifact (a control or question) in a workflow that has uploaded evidence, with its latest version and full version history. The read entry point that complements the per-artifact /documents/{artifactid}/versions (list/upload) and /versions/{id}/download endpoints. View role."
openapi: get /api/v1/v-ciso/organizations/{organizationid}/workflows/{workflowid}/documents
---
