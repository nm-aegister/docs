---
title: "List a workflow's documents"
sidebarTitle: "List Documents"
description: "Discover every artifact in a workflow that has uploaded evidence, with its latest version and full version history. The read entry point that complements the per-artifact /documents/{artifactid}/versions (list/upload) and /versions/{id}/download endpoints. View role."
openapi: get /api/v1/v-ciso/organizations/{organizationid}/workflows/{workflowid}/documents
---
