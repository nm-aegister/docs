---
title: "Struttura delle colonne di un'attività tabella"
sidebarTitle: "Struttura tabella"
description: "Struttura JSON delle colonne di un'attività di tipo tabella: ogni colonna dichiara il proprio type, se è obbligatoria, un format dichiarativo e le choices ammesse (id valore + etichetta localizzata). Le colonne dipendenti portano depends_on e choices_by_parent; le colonne cross-task portano source."
openapi: get /api/v1/v-ciso/organizations/{organizationid}/workflows/{workflowid}/tasks/{taskid}/spec
---
