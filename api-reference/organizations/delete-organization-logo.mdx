---
title: "Delete organization logo"
sidebarTitle: "Delete organization logo"
description: "Remove an organization's logo and favicon: clears `logo_url` and `favicon_url` from the branding and deletes the files. Only super_admin users can delete a logo."
openapi: delete /api/v1/organizations/{id}/branding/logo
---
