---
title: "Get Cloud Defender traffic summary"
sidebarTitle: "Traffic Summary"
description: "Aggregated blocked-traffic figures for one protected domain over a window: totals, the busiest sources, the blocking layer and the daily series.

Blocked requests recorded before the sending address was captured are counted in the total but reported separately, because they cannot be attributed to a source."
openapi: get /api/v1/cloud-defender/{id}/traffic/summary
---
