---
title: "Get Cloud Defender detections"
sidebarTitle: "List Detections"
description: "Retrieve the requests Cloud Defender matched against its attack signatures for one protected domain.

A detection records what a request looked like, not necessarily what happened to it: while a newly protected domain is being tuned its rules run in preview, so a detection can report what would have been blocked. The outcome field states what actually happened."
openapi: get /api/v1/cloud-defender/{id}/security-events
---
