---
title: "List VPN profiles"
sidebarTitle: "List VPN profiles"
description: "List VPN profiles. A user sees the profiles assigned to them; an organization Owner sees every profile in the organizations they own (add `only_mine=true` for just your own). To include the certificate, add `include_cert=true` together with `email` or `only_mine=true`."
openapi: get /api/v1/vpn
---
